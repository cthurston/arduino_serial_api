name: arduino_command_api
description: Arduino Command API is library for communicating with an Arduino board.
version: 1.0.0

environment:
  sdk: '>=2.12.0-0 <3.0.0'

dependencies:
 path: ^1.7.0
 dart_serial_port: 0.0.4+1

dev_dependencies:
  pedantic: ^1.9.0
  test:
